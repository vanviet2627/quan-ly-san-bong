<!DOCTYPE html>
<html lang="vi" class="no-js">
<% include layouts/head %>
<body>
	<% include layouts/header %>

  <div class="container mt-5 pt-5">
    <div>
      <form action="/payment/ewallet" method="POST">
        <div class="row">
          <!-- User info -->
          <div class="col-12 col-md-4">
            <h5>Thông tin liên lạc:</h5>
            <hr>
            <div class="form-row">
              <% if(data.renter){ %>
                <div class="form-group col-12">
                  <label><span class="font-weight-bold">Email:</span> <%= data.renter.email %> </label>
                </div>
              <% } else { %>
                <div class="form-group form-col-6">
                  <label for="email">Email:</label>
                  <input name="email" type="text" id="email" class="form-control" placeholder="Nhập email để đặt sân">
                </div>
              <% } %>
              <% if(data.renter.userName){ %>
                <div class="form-group col-12">
                  <label><span class="font-weight-bold">Tên người dùng:</span> <%= data.renter.userName %> </label>
                </div>
              <% } else { %>
                <div class="form-group form-col-6">
                  <label for="userName">SDT:</label>
                  <input name="userName" type="text" id="userName" class="form-control" placeholder="Nhập tên">
                </div>
              <% } %>
            </div>
          </div>

          <!-- Field info -->
          <div class="col-6 col-md-4">
            <h5>Thông tin thanh toán:</h5>
            <hr>
            <p><span class="mr-1 font-weight-bold">Sân:</span> <%= data.pitch.pitchName %></p>
            <p><span class="mr-1 font-weight-bold">Ngày:</span> <%= data.rentDate.getDay() %></p>
            <p><span class="mr-1 font-weight-bold">Thời gian:</span> <%= data.rentTime %></p>
            <p><span class="mr-1 font-weight-bold">Trong:</span> <%= data.lasting %> giờ</p>
          </div>

          <!-- Payment way -->
          <div class="col-6 col-md-4">
            <h5>Thanh toán:</h5>
            <hr>
            <div class="form-check mt-1">
              <input class="form-check-input" type="radio" name="exampleRadios" id="momo" value="momo" checked>
              <label class="form-check-label" for="momo">
                Mono
              </label>
            </div>
            <div class="form-check mt-1">
              <input class="form-check-input" type="radio" name="exampleRadios" id="zalopay" value="zalopay">
              <label class="form-check-label" for="zalopay">
                Zalopay
              </label>
            </div>
          </div>
        </div>
        <button type="submit" class="primary-btn py-3 text-uppercase d-block mx-auto mt-5 mb-2">Thanh toán</button>
      </form>
    </div>
  </div>
	
	<% include layouts/footer %>
	<% include layouts/script %>
    <script>
      $('#header').addClass('header-scrolled');
    </script>
</body>
</html>
